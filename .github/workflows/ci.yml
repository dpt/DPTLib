---
# CI build for DPTLib
#

name: DPTLib
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: |
          mkdir build && cd build
          cmake ..
          cmake --build .
