---
# CI build for DPTLib
#

name: DPTLib
on: [push]
jobs:
  build:
    name: ${{ matrix.config.name }}
    runs-on: ${{ matrix.config.os }}
    strategy:
      matrix:
        config:
          - { name: "Ubuntu build",  os: "ubuntu-latest",  install: "sudo apt-get install libpng-dev" }
          - { name: "Windows build", os: "windows-latest", install: "vcpkg install libpng pkgconf" }
          - { name: "macOS build",   os: "macos-latest",   install: "" }
    steps:
      - uses: actions/checkout@v4
      - uses: lukka/get-cmake@latest
      - run: ${{ matrix.config.install }}
      - run: |
          mkdir build
          cd build
          cmake -GNinja ..
          cmake --build .
